import Foundation

/// Template for local secrets. Copy to `Secrets.swift` (gitignored) and paste
/// your API token to pre-fill onboarding. Never commit `Secrets.swift`.
enum Secrets {
    /// Default API bearer token used to pre-fill onboarding. Leave empty in shared templates.
    static let defaultAPIToken = ""
}
