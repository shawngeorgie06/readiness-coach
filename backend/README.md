# Readiness Coach Backend

## Prerequisites

- Node.js 20 or newer
- PostgreSQL

## Setup

1. Copy `.env.example` to `.env` and set the database URL and API token.
2. Run `npm install`.
3. Run `npm test`.
4. Start the development server with `npm run dev`.
