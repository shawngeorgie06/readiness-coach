PORT=4000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/readiness_coach
API_TOKEN=dev-token-change-me
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini
TEST_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/readiness_coach_test
CORS_ORIGIN=http://localhost:5173
# Optional. On Render, RENDER_EXTERNAL_URL is set automatically and used for
# self keep-alive pings (every 10 min) so free tier does not sleep at 15 min.
# KEEP_ALIVE_URL=https://your-service.onrender.com/health
